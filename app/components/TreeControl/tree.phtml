{block #tree}
	{foreach $tree as $cat}
		<a class="category {if isset($cat['child'])}menuactual{else}menuhref{/if}" href="{plink Frontend:kategorie 'path'=>$cat['link']}">{$cat['jmeno']}</a>
	{/foreach}
{/block}