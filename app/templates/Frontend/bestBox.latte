  <div class="sidebar-left">
 		<div class="sidebar_top"><h2>Nejprodávanější</h2></div>
 		<div class="sidebar_middle">
 		 {foreach $bests as $item}
 			<div class="polozka">
 				<a href="{plink detail 'path'=>$item->path,'produkt'=>$item->link}">
 				<img src="{$baseUri}/images/uploaded/mini{$item->image}.jpg">
 				<h3>{$item->jmeno|truncate:20}</h3>
 				</a>
				{if $showprices}
 				Cena:&nbsp;{if isset($item->scena) && $item->scena}od&nbsp;{$item->scena|currency:czk:$mena}{else}{$item->cena|currency:czk:$mena}{/if}
				{/if}
 			</div>
 		 {/foreach}
 		</div>
 		<div class="sidebar_bottom"></div>
	</div>
